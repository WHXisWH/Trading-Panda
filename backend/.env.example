# ── Database ──────────────────────────────────────────────────────────────────
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/trading_panda

# ── Redis（Upstash · Python 用 TCP，不是 REST）────────────────────────────────
# 控制台 → 你的数据库 → Redis → Connect → 复制 Python 的 rediss:// 整串：
REDIS_URL=rediss://default:YOUR_UPSTASH_REDIS_PASSWORD@trusted-ghoul-41797.upstash.io:6379

# Cloudflare WebSocket Hub 专用（同一库；勿用于 backend / market-monitor）：
# UPSTASH_REDIS_REST_URL=https://trusted-ghoul-41797.upstash.io
# UPSTASH_REDIS_REST_TOKEN=...

# ── LLM (DeepSeek V3) ────────────────────────────────────────────────────────
DEEPSEEK_API_KEY=sk-replace-me
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat
AGENT_TIMEOUT_SECONDS=3

# ── Sui Blockchain ────────────────────────────────────────────────────────────
SUI_RPC_URL=https://fullnode.testnet.sui.io:443
SUI_PRIVATE_KEY=base64-encoded-private-key
PACKAGE_ID=0xYOUR_PACKAGE_ID
REGISTRY_ID=0xYOUR_REGISTRY_OBJECT_ID

# ── Walrus ────────────────────────────────────────────────────────────────────
WALRUS_PUBLISHER_URL=https://publisher.walrus-testnet.walrus.space
WALRUS_AGGREGATOR_URL=https://aggregator.walrus-testnet.walrus.space

# ── Internal auth (shared with frontend) ─────────────────────────────────────
INTERNAL_SECRET=replace-with-32-byte-random-hex

# ── Runtime ───────────────────────────────────────────────────────────────────
PORT=8000
MAX_ACTORS=100
MERKLE_BATCH_SIZE=50
